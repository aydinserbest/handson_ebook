package github.boniGarcia.testng.ch05.explanations;

public class Explanation_interaction {
    /*
    LoginPage loginPage = new LoginPage(driver);
    HomePage homePage = loginPage.loginAs("testuser", "password");
    assertTrue(homePage.isWelcomeMessageDisplayed());

    Here, LoginPage and HomePage are classes representing respective pages in your application.
    They contain methods that encapsulate the WebDriver interactions.
     */
}
